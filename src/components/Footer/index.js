import styles from "./Footer.module.css"
function Footer(){
    return(
        <footer className={styles.footer}>
            <h2>
                Movies &copy; desenvolvido por Anna Laura
            </h2>
        </footer>

    );
}

export default Footer;
